name: Release

# Cut a release by pushing a version tag, e.g.:  git tag v1.0.0 && git push origin v1.0.0
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write   # needed to create the release and upload the .dmg

jobs:
  build-and-release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Select newest installed Xcode
        run: sudo xcode-select -s "$(ls -d /Applications/Xcode*.app | sort -V | tail -1)/Contents/Developer"

      - name: Build and package the .dmg
        run: bash scripts/package.sh

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" Horizon.dmg \
            --title "Horizon ${GITHUB_REF_NAME}" \
            --notes "Download Horizon.dmg, drag Horizon into Applications, and open it. First launch is blocked by macOS because the app isn't notarized yet: open System Settings > Privacy & Security > Security and click 'Open Anyway', then confirm with your password. See the README for the full first-launch steps and an optional Terminal one-liner. The break is a calm black screen with soft ambient sound."
