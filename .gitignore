# macOS
.DS_Store

# Xcode — build output & user-specific state
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Dependency managers (not used; ignored if ever added)
Pods/
Carthage/Build/

# Bundled break videos — kept out of the public repo to avoid redistributing
# third-party stock footage. The app falls back to a gradient when none are present.
Horizon/Horizon/BreakVideos/

# Misc
*.log
